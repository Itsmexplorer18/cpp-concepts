int main(){
  /*lambda expression will have [capture clause](parameters){
  and the body here
};
the capture clause is to capture the variables and functions within the curr scope
& is used for passing those variables by reference 
= is used for passing by value you cannot modify them if you pass  by value
*/
  return 0;
}
